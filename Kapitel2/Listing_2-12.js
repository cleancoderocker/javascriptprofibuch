/* Funktioniert nur in ES6 */
var operationen = {
    addition(x, y) {
        return x + y;
    },
    subtraktion(x, y) {
        return x - y;
    },
    multiplikation(x, y) {
        return x * y;
    },
    division(x, y) {
        return x / y;
    }
}